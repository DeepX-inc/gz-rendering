name: DeepX CI

run-name: Build Docker container and perform unit testing

on: [push, workflow_dispatch]

jobs:
  build-library:
    runs-on: ubuntu-20.04
    name: Build and test
    steps:
      - name: Checkout repo
        uses: actions/checkout@v3

      - name: Login to Github Container Registry
        uses: docker/login-action@v1
        with:
          registry: ghcr.io
          username: deepx-inc
          password: ${{ github.token }}

      - name: Build and commit the Docker container
        run: |
          docker build --build-arg BASE_IMAGE=ghcr.io/deepx-inc/base_images --build-arg TAG=foxy -t deepx-gz-rendering .

      - name: Run tests
        run: |
          docker run --name deepx-gz-rendering \
          -v $GITHUB_WORKSPACE:/root/gazebo \
          --entrypoint bash \
          -w /root/gazebo \
          deepx-gz-rendering-image \
          -c 'ls /; ls /root; ls/root/gazebo/; ls /root/gazebo/gz-rendering; cd /root/gazebo/gz-rendering/build; ctest'
